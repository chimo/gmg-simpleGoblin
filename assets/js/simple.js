( function( $ ) {
    /* exif dropdown */
    $( "#exif_additional_info_button" ).on( "click", function() {
        $( "#exif_additional_info" ).slideToggle( "slow" );
    } );

    /* TODO - maps */
}( jQuery ) );

